namespace TennisLeague.DataAccess.Models
{
    // This enum should reflect the values in the LeagueSeasonStatus table
    public enum SeasonStatus
    {
        New = 1,
        OpenRegistration = 2,
        Active = 3,
        Completed = 4
    }
}
