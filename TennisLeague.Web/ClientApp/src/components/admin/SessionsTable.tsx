import { Session } from "./Session";

type Props = {
    sessions: Session[];
    onDeleteClick: () => void;
    onEditClick: () => void;
}

const SessionsTable = (props: Props) => {
    return (
    <>
    </>
    );
}

export default SessionsTable;